# Split string

# The split() method returns a list where the text between
# the specified separator becomes the list items.

#The split() method splits the strings  into substrings
# if it finds instances or the separator:

a = "Hello, World!"
print(a.split(","))
